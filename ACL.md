# ACL.md — ตารางสิทธิ์การเข้าถึงระบบ (JaiFit)

ระบบ JaiFit ตอนนี้มีบทบาทผู้ใช้ **1 บทบาท** คือ `user` (ผู้ใช้ที่ล็อกอินแล้ว บันทึกมื้ออาหารของตัวเอง) โดยมีสถานะ **ไม่ได้ล็อกอิน (guest)** เป็นอีกกรณีหนึ่งที่ต้องระบุสิทธิ์ไว้ด้วย

> อ้างอิงจากพฤติกรรมจริงของ [meallogs.html](app/meallogs.html) ณ วันที่จัดทำ — ดูหมายเหตุข้อจำกัดท้ายเอกสาร

## ตารางสิทธิ์

| การกระทำ | 🚫 Guest (ไม่ได้ล็อกอิน) | 👤 user (ล็อกอินแล้ว) |
|---|---|---|
| สมัครสมาชิก (สร้างบัญชี + `users/{uid}`) | ✅ ทำได้ | — (มีบัญชีแล้ว) |
| เข้าสู่ระบบ / ออกจากระบบ | ✅ เข้าสู่ระบบได้ | ✅ ออกจากระบบได้ |
| ดูรายการ mealLogs | ❌ ทำไม่ได้ (ถูกเด้งไปหน้าเข้าสู่ระบบ) | ✅ ทำได้ **เฉพาะของตัวเอง** เท่านั้น |
| เพิ่มมื้ออาหารใหม่ | ❌ ทำไม่ได้ | ✅ ทำได้ (บันทึกเป็นของตัวเองเท่านั้น — `userId` ผูกกับบัญชีที่ล็อกอินอยู่ เลือกเป็นคนอื่นไม่ได้) |
| เปลี่ยนสถานะ mealLog (safe/risky/exceeded) | ❌ ทำไม่ได้ | ✅ ทำได้ **เฉพาะของตัวเอง** เท่านั้น |
| ลบ mealLog | ❌ ทำไม่ได้ | ✅ ทำได้ **เฉพาะของตัวเอง** เท่านั้น |
| แก้ไขข้อมูลอื่นของ mealLog (title, description, foodCategory ฯลฯ) | ❌ ทำไม่ได้ | ❌ ทำไม่ได้ (แอปไม่มีปุ่ม/ฟอร์มแก้ไขฟิลด์เหล่านี้หลังบันทึกแล้ว) |
| กำหนด/เปลี่ยน role ของผู้ใช้ (ตัวเองหรือคนอื่น) | ❌ ทำไม่ได้ | ❌ ทำไม่ได้ (ไม่มี UI ตั้งค่า role — ทุกบัญชีใหม่ได้ `role: "user"` ตายตัวตอนสมัคร) |

## หมายเหตุ / ข้อจำกัดที่ควรทราบ

- **อัปเดต:** `firestore.rules` (repo root) ถูก deploy แล้ว บังคับ `request.auth != null` สำหรับทุกการอ่าน/เขียนในทุก collection — ยืนยันแล้วว่า request ที่ไม่ได้ล็อกอินโดน Firestore ปฏิเสธด้วย `403 PERMISSION_DENIED` จริง ดังนั้นสิทธิ์ "ทำไม่ได้" ของ Guest ในตารางนี้**ถูกบังคับทั้งฝั่ง UI และฝั่งฐานข้อมูล** ไม่ใช่แค่ปิดกั้นที่ JavaScript เหมือนก่อนหน้านี้แล้ว — ต่อให้เรียก Firestore ตรงๆ (เช่นผ่าน DevTools หรือ REST API) โดยไม่ล็อกอิน ก็จะถูกปฏิเสธเช่นกัน
- **`mealLogs` มี ownership restriction แล้ว** ทั้งอ่าน/สร้าง/แก้/ลบ — `firestore.rules` เช็คว่า `resource.data.userId == request.auth.uid` (สร้างเช็คจาก `request.resource.data.userId`) ผู้ใช้แต่ละคนเห็นและแก้ไขได้แค่ mealLog ของตัวเองเท่านั้น ไม่เห็น/แก้ของคนอื่นอีกต่อไป
- Subcollection `nudges` ใต้ mealLog (ยังไม่มี UI ใช้งานในแอปตอนนี้) ก็ถูกกันด้วย ownership เช่นกัน — เพราะ nudge เองไม่มี field `userId`, rule จึงใช้ `get()` ย้อนไปเช็ค `userId` ของ mealLog แม่แทน
- ยังไม่มีบทบาท `admin` หรือบทบาทอื่นในระบบ — ช่อง `role` ใน `users/{uid}` มีค่าเดียวคือ `"user"` เตรียมไว้สำหรับขยายบทบาทในอนาคต
